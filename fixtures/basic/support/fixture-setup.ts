// Stands in for a real project's own test setup. Its only job is to prove
// it still ran when a depug wrapper config adds a second setupFiles entry
// alongside this one.
globalThis.__FIXTURE_SETUP_RAN__ = true;
